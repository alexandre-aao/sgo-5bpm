# Dev local: Vite roda em :5173, a API (server.js) roda separada em :3005.
# Em produção (quando o build do client for servido pelo próprio server.js) a variável
# fica vazia e as chamadas usam o mesmo origin — ver client/src/lib/api.ts.
VITE_API_BASE_URL=http://localhost:3005
